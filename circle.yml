machine:
  services:
    - docker
dependencies:
  override:
    - docker build -t gaiaadm/haproxy .
    - docker tag gaiaadm/haproxy gaiaadm/haproxy:$CIRCLE_BUILD_NUM
test:
  override:
    - /bin/true
deployment:
  hub:
    branch: master
    commands:
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push gaiaadm/haproxy
general:
  branches:
    only:
      - master
notify:
  webhooks:
    - url: http://beta.gaiahub.io:88/wh/5a866f9b-98c9-438d-a8d4-53cf4ae9599c/4c310c8e220ee9bead74eece42ce0e109fd3535e
